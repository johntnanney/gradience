# Changelog

## v2-revision-length — 2026-04-22 (mild trim + three-instances-two-kinds synthesis + Option B tightening; main text 13 pp)

Revision-length pass against a 10–12 pp position/methods venue target. Four categories of change. (1) References.bib hygiene: removed `@article`/`@inproceedings`/`@misc` tokens from the header comment block and the Panahi TODO stub — BibTeX parses `@` as structural even inside `%` comments, which was silently dropping the first four entries and erroring on the TODO. Canonical repo now compiles cleanly. (2) Mild-trim package: §5.1/§5.2/§5.3/§5.4 tails tightened; §7.4 psychometric-tradition reference compressed from 14 lines to 8; §8 objections merged "null would be less compelling" + "rank-on-residuals niche" into a single two-form `\paragraph{``The case study is weaker than the framework.''}`. (3) Three-instances-two-kinds paragraph inserted at §6.4 close: articulates the dialectic that the paper's three framework findings (rank-on-residuals property, ICC = 0.566, H1 null) split into two epistemic kinds — *discovery* (new measurement property surfaced) and *calibration-and-modesty* (existing claims bounded) — and argues that both kinds together are what measurement discipline is *for*. Paragraph-insertion variant chosen over structural-recast variant. (4) Option B second-pass tightening: §5 preamble compressed from 6 lines to 2 (removed procedural metatext); §7.2 + §7.3 merged into single subsection "What this changes, and what it does not" — argumentative improvement independent of page math because the two previously-separated subsections shared a rhetorical move that reads stronger combined. Final state: main text §§1–9 spans 13 pp (ends ~81% down p13); total PDF 18 pp (13 main + ~2 refs + 4 appendix). User accepted 13 pp main as final. See `revision_notes.md` RN-017 for the full execution record and the list of items explicitly deferred to v3 (§2 rewrite per `thesis_memo.md`, §1.1 second-example citation, Panahi attribution).

## v2-icc-computed — 2026-04-22 (Appendix-D TODO closed; ICC = 0.566, SEM = 0.014)

Implementation of RN-015's spec. `scripts/n134/09_analysis_icc.py` produces `sidecar/results/n134/analysis_icc.json`: ICC(2,1) absolute-agreement single-measurement on the 8×3 same-task panel, with Shrout–Fleiss primary CI and block-bootstrap over tasks (5000 resamples, seed 134) secondary. Direct Shrout–Fleiss implementation — no `pingouin` dependency; `scipy.stats.f` for the F-distribution CI; `compute_s_h1` imported from `06_analysis_h1.py` so the instrument-under-reliability-estimation is bit-identical to the instrument-under-H1-test. Results: ICC = 0.566 (moderate); Shrout–Fleiss 95% CI [0.165, 0.874]; bootstrap 95% CI [0.141, 0.965]; CI agreement *divergent* at 0.091 on the upper bound, both reported per spec §3.2; SEM = 0.014. Three prose insertions applied to `draft_v2_thesis_b.tex` per spec §8 templates (Appendix D, §2.2, §4.2). The `[TODO]` in Appendix D is closed; no explicit placeholder remains in the first-pass .tex. See `revision_notes.md` RN-016 for the full implementation record and the one honesty-caveat (SEM-transferability from same-task to cross-task pairs) added to the Appendix-D prose beyond the spec template.

## v2-icc-spec — 2026-04-22 (cross-seed ICC specification filed)

`sidecar/notes/n134_icc_spec.md` committed as the pre-implementation specification for the cross-seed ICC computation that closes the explicit `[TODO]` in `app:reliability`. The spec commits to ICC(2,1) absolute-agreement single-measurement, Shrout–Fleiss F-distribution CI as primary with block-bootstrap over tasks as secondary, and SEM reported alongside — each with a short measurement-theoretic defense rather than as a silent methodological choice. Implementation (the script, the JSON output, the Appendix-D paragraph, and the §2.2 / §4.2 in-text insertions) is the follow-on work. The spec exists separately from the implementation specifically so that the design choices are reviewable as prose; writing the spec before the script is the paper eating its own dogfood. See `revision_notes.md` RN-015.

## v2-§1.1-examples — 2026-04-22 (two concrete reporting-gap examples added)

§1.1 ("The reporting gap") restructured from two paragraphs to four. Para 1 states the pattern abstractly and announces two examples; para 2 develops Zhou et al. 2026's $r = 0.572$ as the LoRA-merging example, annotating the three measurement-theoretic absences (reliability, tolerance, confound decomposition) against that specific report; para 3 describes capability-evaluation reporting (MMLU / HellaSwag / BBH to two or three decimals without cross-seed or cross-prompt reliability coefficients) as the cross-subfield example, making the "same gap in a subfield with no methodological contact with merging" argument explicit; para 4 preserves the original three-gap synthesis. The `rahamim2026mergeability` citation previously in §1.1 is no longer used there (remains cited at §8). The capability-evaluation paragraph is argumentatively self-sufficient but marked with a LaTeX comment identifying candidate directions for a specific cited claim at revision time. See `revision_notes.md` RN-014.

## v2-memo — 2026-04-22 (thesis memo filed as pre-§2 anchor)

`thesis_memo.md` committed as the intellectual anchor document preceding §2 revision. One-page, three-paragraph structure: the central claim with the argued-against view named explicitly (direct-readout vs. inferential / fallible-indicator measurement); the four framework components reframed as *jointly constitutive* of the inferential view rather than as independent methodological virtues; the three contributions articulated in a normative/demonstrative/productive dialectical structure. Closes the v2 outline's immediate-action 1. The memo is diagnostic as well as descriptive: its successful compression to one page (under the stated philosophical positions) indicated that §2 needs *restructuring* — to a continuous argument tracking the four questions the inferential view requires answered — rather than expansion of the existing four-parallel-subsection layout. See `revision_notes.md` RN-013 for the full record of the decision and its staged work plan. `draft_v2_thesis_b.tex` is unchanged at this stage; the §2 rewrite is queued as a separate revision pass so the memo-as-blueprint stays distinct from the prose work that will track it.

## v2-outline — 2026-04-22 (repositioning to Thesis B paper)

Revision-period artifact. `draft_v2_thesis_b_outline.md` committed as the first planning document for the Thesis B repositioning: from "N134 findings paper" (v1 workshop-paper skeleton) to "measurement-discipline framework paper with N134 as extended worked example." Supersedes the v1 skeleton for revision planning; v1 is retained as `draft_v1.md` per consolidation convention (frozen reference; do not edit). The outline calibrates a longer target (8–10 pages, position-paper / methods-paper venue rather than workshop-paper) and a restructured section plan that puts the framework (§§1–2) before the worked example (§§3–5) and adds a generalization section (§7) and objections-handling section (§8).

`draft_current.md` is unchanged at this stage — the outline is planning, not revision prose. When prose drafting begins against the Thesis B structure, `draft_current.md` becomes the working file for that prose; the outline remains the planning reference. See `revision_notes.md` RN-012 for the scope decision to keep N135-alt out of this paper (standalone Thesis B, N134 material only).

## v1.0.1 — 2026-04-20 (attribution corrections; findings unchanged)

Post-T4 correction pass. All changes are attribution / citation metadata only; no N134 finding, number, figure, or deviation changes. The `n134-report-v1` tag is moved forward to the post-correction commit; the commit range leading to v1.0.1 is traceable via `git log`.

- RN-001 (Zhou et al. r = 0.572): citation resolved via `sidecar/notes/n134_spec.md` trace. Paper is Zhou et al. 2026, arXiv:2601.22285 ("Demystifying Mergeability..."). Bib entry added.
- RN-002: retracted. My initial MergeBench-misattribution claim was based on a misread of positioning material.
- RN-003 (Panahi attribution): softened to citation-by-title across `docs/positioning/gradience_differentiation.md`, `docs/THEORY.md`, `docs/technical-report.md`. Attribution flagged as unverified pending OpenReview access.
- RN-004 (SVC citation): resolved via arXiv fetch. Paper is Li et al. 2026, arXiv:2602.05536 ("When Shared Knowledge Hurts..."). Bib entry updated with full author list.
- RN-005: SVC code-audit fallback documented but not triggered (RN-004 made it unnecessary).
- RN-006: **"Cocchieri et al." → "Hitit et al."** attribution correction. The paper cited throughout Gradience materials (arXiv:2511.21437) is actually by Hitit, Girrbach, Akata. Corrected in `sidecar/notes/n134_spec.md`, `sidecar/notes/n134_report.md`, `docs/THEORY.md`, `docs/positioning/gradience_differentiation.md`. Archive (`sidecar/notes/archive/n134_spec_v3_update.md`) left as-written.

## v1 — 2026-04-20

Initial skeleton committed. `draft_v1.md` contains structure, intended section lengths, and figure plan only — no paper prose. See RN-000 for the reasoning. Corresponds to report tag `n134-report-v1` (pre-correction) on commit `3ba4270`.

**Target-venue word-count sanity check (2026-04-20):** the skeleton's stipulated section lengths (Abstract 150w; Intro 1p; Methods 1p; H1 1p; Replications 0.5p + fig; Comparison 0.75p + fig; Discussion 1p + fig; Conclusion 0.25p) sum to approximately 4,100 words plus 4 figures — roughly 6.8 pages at the NeurIPS-style ~600 words/page rate. This is in range for a short-paper workshop (4–6 pages) but tight for a full 8-page workshop. Revision-time may surface real word counts materially above or below this target; if so, recalibrate the section allocation before writing rather than after. Flag for the first revision session.
