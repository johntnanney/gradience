# Focused validation: r32 probe, seed 42
# For defensible statistics - run per_layer only

model:
  name: distilbert-base-uncased

task:
  dataset: glue
  subset: sst2
  metric: accuracy
  
train:
  max_steps: 200  
  eval_steps: 50   
  save_steps: 50   
  
  lr: 0.00005  
  per_device_train_batch_size: 8
  per_device_eval_batch_size: 32
  weight_decay: 0.01
  seed: 42  # Seed 1 of 3
  
  train_samples: 800
  eval_samples: 200   

lora:
  probe_r: 32
  alpha: 32
  dropout: 0.0
  target_modules: ["q_lin","k_lin","v_lin","out_lin"]

compression:
  allowed_ranks: [2, 4, 8, 16, 32]
  acc_tolerance: 0.025

runtime:
  device: cpu

bench_version: "0.1"
run_type: "focused_seed42"